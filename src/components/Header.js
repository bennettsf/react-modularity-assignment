import React from 'react'

export default function Header() {
    const headerText = "Bennett's Modularity Assignment"
    return (
        <header>
            <h1>{headerText}</h1>
        </header>
    )
}